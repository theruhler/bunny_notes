<Window x:Class="BunnyNotes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bunny Notes" Height="450" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">
    
    <Grid>
        <Image x:Name="BunnyImage" Stretch="Uniform" />

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,20,0,0">
            <TextBlock x:Name="NoteText" 
                       TextWrapping="Wrap" 
                       TextAlignment="Center"
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="#444"
                       MaxWidth="250"
                       FontFamily="Segoe Print"/>
            
            <Button Content="Close" Click="Close_Click" Margin="0,10,0,0" 
                    Background="Transparent" BorderThickness="0" Foreground="Gray" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
